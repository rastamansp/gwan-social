# Opcional: liga a SPA à API Nest (`apps/api`). Vazio = dados mock locais.
# Base até /api/v1 (sem barra final). CORS na API deve incluir http://localhost:5173
VITE_API_URL=http://localhost:4000/api/v1
